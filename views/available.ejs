<!DOCTYPE html>
<html lang="en">
  <head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>videoChatApp</title>
  <style>
  ul{
     display:inline-block;
      margin: auto;
  }
  li{
     white-space:nowrap;
    display: grid;
    align-items: center;
    padding: 10px 30px 10px 10px;
    margin: 10px;
    overflow: hidden;
    border-radius: 10px;
    box-shadow: 0 5px 7px -1px rgba(51, 51, 51, 0.23);
    cursor: pointer;
    transition: transform .25s cubic-bezier(.7,.98,.86,.98), box-shadow .25s cubic-bezier(.7,.98,.86,.98);
    background-color: #E0E0E0;
    
    &:hover {
      transform: scale(1.2);
      box-shadow: 0 9px 47px 11px rgba(51, 51, 51, 0.18);
    }
  }
  a{
  color:#000;
  text-align: center;
  }
  </style>
  <script>
      const AGENT_ID = "<%= agent %>";
    </script>
  </head>
<body style="background: rgba(59, 130, 246, 0.5);         text-align: center; ">
  <h2 style-"display:inline-block;
      margin: auto;">Active Rooms</h2>
  <ul >
    <% for (var i = 0; i < avail_rooms.length; i++) { %>
    <li class="room_list">
      <a href="/call/?user=agent&id=<%=avail_rooms[i] %>&agentid=<%=agent%>"/>
        <%= avail_rooms[i] %>
    </li>
    <% } %>
</ul>
</body>
</html>

